name: validate-design-os
on:
  push:
  pull_request:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
      - uses: actions/setup-python@v7
        with:
          python-version: '3.x'
      - run: npm install --ignore-scripts --package-lock=false
      - run: npm run validate
      - run: npm run eval:skills
      - run: npm run test:unit
  browser-hard-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
      - run: npm install --ignore-scripts --package-lock=false
      - run: npx playwright install --with-deps chromium
      - name: Nocturne accessibility and browser gates
        run: npx playwright test tests/release-gates.spec.ts --project=edge-mobile --project=desktop
        env:
          TARGET_URL: http://127.0.0.1:4173
          STATIC_DIR: experiments/nocturne-concierge
          STATIC_PORT: '4173'
      - name: Linehold accessibility and browser gates
        run: npx playwright test tests/release-gates.spec.ts --project=edge-mobile --project=desktop
        env:
          TARGET_URL: http://127.0.0.1:4174
          STATIC_DIR: experiments/linehold-forge
          STATIC_PORT: '4174'
      - name: Afterimage Atlas browser gates
        run: npx playwright test tests/afterimage.spec.ts --project=edge-mobile --project=desktop
        env:
          TARGET_URL: http://127.0.0.1:4176
          STATIC_DIR: experiments/afterimage-atlas
          STATIC_PORT: '4176'
      - name: Sequence Desk browser gates
        run: npx playwright test tests/sequence-desk.spec.ts --project=edge-mobile --project=desktop
        env:
          TARGET_URL: http://127.0.0.1:4177
          STATIC_DIR: experiments/sequence-desk
          STATIC_PORT: '4177'
      - name: Orbital Commons browser gates
        run: npx playwright test tests/orbital-commons.spec.ts --project=edge-mobile --project=desktop
        env:
          TARGET_URL: http://127.0.0.1:4178
          STATIC_DIR: experiments/orbital-commons
          STATIC_PORT: '4178'
      - name: Launch site accessibility and browser gates
        run: npx playwright test tests/release-gates.spec.ts tests/launch-site.spec.ts --project=edge-mobile --project=tablet --project=desktop
        env:
          TARGET_URL: http://127.0.0.1:4175
          STATIC_DIR: site
          STATIC_PORT: '4175'
