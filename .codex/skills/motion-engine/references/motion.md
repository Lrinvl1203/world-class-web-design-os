# Motion notes

Use Motion for React when visual state follows React state or layout. High-value capabilities include layout animation, shared `layoutId`, gestures, `AnimatePresence`, scroll-linked values and reduced-motion hooks.

Do not use Motion merely to replace a two-line CSS hover transition. Keep motion values and transforms localized; use lazy loading/bundle-reduction guidance when animation surface is large.
